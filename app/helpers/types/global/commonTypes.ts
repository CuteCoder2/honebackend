export enum stateType  {
    pen = "pending",
    rej  = "rejected",
    val  = "validated"
}

export enum osType {
    and = "android",
    ios = "IOS"
}

export enum simType {
    na = "nano",
    du = "dual"
}

export enum productType {
    mobile = "mobile",
    fixedtel = "fixed phone",
    pc = "computer"
}

export enum productCat  {
    elec = "electronic",
    
}