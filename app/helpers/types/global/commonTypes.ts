export enum stateType  {
    pen = "pending",
    rej  = "rejected",
    val  = "validated"
}

export enum osType {
    and = "android",
    ios = "IOS"
}

export enum simType {
    na = "nano",
    du = "dual"
}

export enum productType {
    mobile_p = "mobile phone",
    fixed_p = "fixed phone",
    pc = "computer"
}

export enum productCat  {
    elect = "electronic",
    
}